highway_to_hell
    Minh Ngo <nlminhtl . at # gMa1l d0t com>

This application is based on the trigrams frequency approach

Dependencies:
    g++, boost filesystems, boost regex, boost system

Building:
    make

Train data:
    pushd wiki
    ./unpack_data.sh
    popd
    make clean && make && make gen

How to run
    $ ./trigram_calc -detect <file name> 
